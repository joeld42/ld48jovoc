#include <allegro.h>

#include "gfxutil.h"

void metal_box( BITMAP *targ, 
			    int x0, int y0,
				int w, int h )
{
	static BITMAP *cornerImg=NULL, *fillImg=NULL;

	// initialize
	if (!cornerImg) {
		//cornerImg = load_bitmap( "Graphics/metal_corner.bmp"
	}
}


